#include <iostream>
using namespace std;

int f(int*arr, int first, int last)
{
int min=arr[0];
int temp=0;
for(;first<last;++first)
{
if (arr[0]<arr[first])
{ temp=temp+1;
}}
return temp;
	
}
int main() 
{
	int array[15]{1,2,3,4,1,9,8,10,1,1,1,1,1,1,1};
	cout<<f(array,0,15);
	return 0;
}
